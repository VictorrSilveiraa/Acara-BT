# Rota Certa

Painel local e responsivo para registrar, comparar e acompanhar preços de passagens aéreas.

## Executar

```bash
python3 -m http.server 4173
```

Abra `http://localhost:4173`. Os registros são preservados no `localStorage` do navegador.
